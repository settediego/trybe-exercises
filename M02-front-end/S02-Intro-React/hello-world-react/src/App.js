import React from 'react';
import AboutMe from './About';

class App extends React.Component {
  render() {
    return (
      <AboutMe />
    );
  }
}

// React é uma biblioteca
// serve para criar interfaces
// cria componentes com JSX para criar interface
// Todo componente no React é com letra maiúscula
// Dentro do componente React temos o JSX(que parece com HTML)
// .js e .jsx são a mesma coisa
// para colocar código JS no JSX usamos o { variavel }
// 2 formas de criar os componentes: função e outra com classes

export default App;
